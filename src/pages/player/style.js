import styled from "styled-components";

export const LSPlayerWrapper = styled.div`
    background-color: rgba(0,0,0,0.6);
    text-align: center;

    button{
        background-color: #ff0;
        cursor: pointer;
    }

    span {
        background-color: #ff0;
        margin: 0 0 0 20px;
    }

    /* .ant-slider-handle {
        background-color: #f00;
        transition: 1s;
    } */
`